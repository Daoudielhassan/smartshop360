{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 4, "w": 6, "x": 0, "y": 0 },
      "id": 1,
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] }, "orientation": "horizontal" },
      "title": "ETL Runs (success)",
      "type": "stat",
      "targets": [
        {
          "expr": "sum(smartshop_etl_runs_total{status='success'})",
          "legendFormat": "Succès"
        }
      ]
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 4, "w": 6, "x": 6, "y": 0 },
      "id": 2,
      "options": { "reduceOptions": { "calcs": ["lastNotNull"] } },
      "title": "Durée ETL (moyenne)",
      "type": "stat",
      "targets": [
        {
          "expr": "smartshop_etl_duration_seconds_sum / smartshop_etl_duration_seconds_count",
          "legendFormat": "Durée"
        }
      ]
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 4, "w": 6, "x": 12, "y": 0 },
      "id": 3,
      "title": "Requêtes Agent IA",
      "type": "stat",
      "targets": [
        {
          "expr": "sum(rate(smartshop_agent_requests_total[5m])) * 60",
          "legendFormat": "req/min"
        }
      ]
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 4, "w": 6, "x": 18, "y": 0 },
      "id": 4,
      "title": "Alertes envoyées",
      "type": "stat",
      "targets": [
        {
          "expr": "sum(smartshop_alerts_sent_total)",
          "legendFormat": "Total"
        }
      ]
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 4 },
      "id": 5,
      "title": "Durée requêtes SQL (p95)",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(smartshop_query_duration_seconds_bucket[5m])) by (le, query_type))",
          "legendFormat": "{{query_type}} p95"
        }
      ]
    },
    {
      "datasource": "Prometheus",
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 4 },
      "id": 6,
      "title": "Checks qualité données",
      "type": "timeseries",
      "targets": [
        {
          "expr": "rate(smartshop_dq_checks_total[5m])",
          "legendFormat": "{{result}}"
        }
      ]
    }
  ],
  "refresh": "30s",
  "schemaVersion": 38,
  "tags": ["smartshop360"],
  "templating": { "list": [] },
  "time": { "from": "now-24h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "SmartShop 360 — Overview",
  "uid": "smartshop360-overview",
  "version": 1
}
